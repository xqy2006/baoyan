# Copy this file to .env and adjust secure values. NEVER commit your real .env.

# --- Database ---
DB_ROOT_PASSWORD=ChangeRootPwd123!
DB_NAME=xmudemo
DB_HOST=db
DB_USERNAME=appuser
DB_PASSWORD=ChangeAppPwd456!

# --- Application ---
APP_PORT=8080
# (Optional) override full JDBC url; otherwise DB_URL is derived in compose
# DB_URL=jdbc:mysql://db:3306/xmudemo?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8

# --- Security ---
# Generate something long & random (at least 64 chars)
JWT_SECRET=PLEASE_REPLACE_WITH_A_LONG_RANDOM_SECRET_VALUE_64+CHARS

# --- JVM Options ---
JAVA_OPTS=-Xms256m -Xmx512m

# Optional overrides
# SPRING_PROFILES_ACTIVE=prod
# APP_UPLOAD_DIR=/app/uploads

