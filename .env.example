# ==============================
# 保研小助手 Docker 环境变量示例
# 复制为 .env 并根据需要修改
# ==============================

# 应用访问端口 (映射到宿主机 8080)
APP_PORT=8080

# 数据库基础信息
DB_NAME=xmudemo
DB_USERNAME=appuser
DB_PASSWORD=AppChangeMe123!
DB_ROOT_PASSWORD=RootChangeMe123!

# JWT 密钥 (务必修改为足够长且随机的字符串)
JWT_SECRET=REPLACE_WITH_A_VERY_LONG_RANDOM_SECRET_STRING_>=64_CHARS

# JVM 参数 (可选)
JAVA_OPTS=-Xms512m -Xmx512m -XX:+UseG1GC

# 前端如果需要跨域开发，可在运行 docker-compose 之前设置 (可选)
# APP_CORS_ORIGINS=http://localhost:3000

